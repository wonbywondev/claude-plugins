---
description: Diagnose preserve-session state for the current project
allowed-tools: Bash(bash:*)
---

Run the following and show the output:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/hooks/doctor.sh"
```
